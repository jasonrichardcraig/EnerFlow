<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:selectors="clr-namespace:EnerFlow.Selectors"
                    xmlns:converters="clr-namespace:EnerFlow.Converters"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Converter for converting boolean value to visibility -->
    <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    
    <!-- Converter for converting inverted boolean value to visibility -->
    <converters:InvertedBooleanToVisibilityConverter x:Key="InvertedBooleanToVisibilityConverter"/>

    <!-- DataTemplate for the Map tab header -->
    <DataTemplate x:Key="MapTabHeaderDataTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Stretch="None" Source="/Images/Maps_24x24.png"/>
            <TextBlock Text="Map" VerticalAlignment="Center"/>
        </StackPanel>
    </DataTemplate>

    <!-- DataTemplate for the Setup tab header -->
    <DataTemplate x:Key="SetupTabHeaderDataTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Margin="0,0,4,0" Source="/Images/Tools_24x24.png"/>
            <TextBlock Text="Setup" VerticalAlignment="Center"/>
        </StackPanel>
    </DataTemplate>

    <!-- DataTemplate for the Data Entry tab header -->
    <DataTemplate x:Key="DataEntryTabHeaderDataTemplate">
        <StackPanel Orientation="Horizontal">
            <Image Source="/Images/Runs_24x24.png"/>
            <Label>Data Entry</Label>
            <DatePicker IsEnabled="{Binding ElementName=DataEntryTab, Path=IsSelected}"></DatePicker>
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="RootTreeViewItemHeaderDataTemplate">
        <Image Height="16" Width="16" Margin="0,0,4,0" Source="/Images/Root.png"/>
    </DataTemplate>

    <HierarchicalDataTemplate x:Key="CompanyTreeViewItemDataTemplate" ItemsSource="{Binding Children}">
        <StackPanel Orientation="Horizontal">
            <Image Height="16" Width="16" Margin="0,0,4,0" Source="/Images/Company_16x16.png"/>
            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
        </StackPanel>
    </HierarchicalDataTemplate>

    <HierarchicalDataTemplate x:Key="DistrictTreeViewItemDataTemplate" ItemsSource="{Binding Children}">
        <StackPanel Orientation="Horizontal">
            <Image Height="16" Width="16" Margin="0,0,4,0" Source="/Images/District.png"/>
            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
        </StackPanel>
    </HierarchicalDataTemplate>

    <HierarchicalDataTemplate x:Key="AreaTreeViewItemDataTemplate" ItemsSource="{Binding Children}">
        <StackPanel Orientation="Horizontal">
            <Image Height="16" Width="16" Margin="0,0,4,0" Source="/Images/Area.png"/>
            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
        </StackPanel>
    </HierarchicalDataTemplate>

    <HierarchicalDataTemplate x:Key="FieldTreeViewItemDataTemplate" ItemsSource="{Binding Children}">
        <StackPanel Orientation="Horizontal">
            <Image Height="16" Width="16" Margin="0,0,4,0" Source="/Images/Field.png"/>
            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
        </StackPanel>
    </HierarchicalDataTemplate>

    <selectors:HierarchyTreeViewItemTemplateSelector x:Key="HierarchyTreeViewItemTemplateSelector" 
                                                     CompanyTemplate="{StaticResource CompanyTreeViewItemDataTemplate}"
                                                     DistrictTemplate="{StaticResource DistrictTreeViewItemDataTemplate}"
                                                     AreaTemplate="{StaticResource AreaTreeViewItemDataTemplate}"
                                                     FieldTemplate="{StaticResource FieldTreeViewItemDataTemplate}">

    </selectors:HierarchyTreeViewItemTemplateSelector>

</ResourceDictionary>